<div class="container-fluid">
  <div class="row justify-content-center">
    <div class="col-lg-6 mt-2">
        <div class='reference-title mb-0 h5'>
          <%= "Rule #{@reference.label}" %>
        </div>
    </div>
  </div>

  <div class="row justify-content-center">
    <div class="col-lg-6">
      <%= render @questions %>
      <% unless @pagy.next.nil? %>
        <%= render "next_page" %>
      <% end %>
    </div>
  </div>
</div>
