<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
  <div class="container-fluid mx-md-3">
    <a class="navbar-brand" href="/questions">RefNetwork</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="user-container collapse navbar-collapse" id="navbarText">
      <ul class="navbar-nav ms-auto">
        <% if user_signed_in? %>
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle text-white" href="#" id="navbarUserDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= current_user.first_name %> <%= current_user.last_name %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarUserDropdown">
              <li><%= button_to 'Log out', destroy_user_session_path, method: :delete, class: 'dropdown-item btn btn-link text-decoration-none' %></li>
            </ul>
          </li>
        <% else %>
          <li class="nav-item">
            <%= button_to 'Log in', new_user_session_path, method: :get, class: 'btn btn-link text-decoration-none text-white' %>
          </li>
          <li class="nav-item">
            <%= button_to 'Sign up', new_user_registration_path, method: :get, class: 'btn btn-link text-decoration-none text-white'  %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>