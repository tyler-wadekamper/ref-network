<p>
  <div>
    Question
    <% if question.author == current_user %>
      <%= link_to 'Edit', edit_question_path(question.id) %>
    <% end %>
  </div>
  <%= question.body %>

  <%= render 'answers/answer', answer: question.answer %>
</p>
